services:
  - type: web
    name: ai-interview-preparation-platform
    runtime: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -Dserver.port=$PORT -jar target/ai-interview-preparation-platform-1.0.0.jar
    healthCheckPath: /
